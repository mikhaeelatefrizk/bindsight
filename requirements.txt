# Streamlit Cloud deploy requirements (for share.streamlit.io).
# Mirrors `pip install -e ".[discover,report]"` so the cloud deployment has
# everything the web demo needs. Local installs should prefer the editable
# install via pyproject.toml.

# ---- bindsight itself ----
-e .

# ---- discovery half ----
pydeseq2>=0.5
pandas>=2.2
pyarrow>=16
numpy>=1.26
biopython>=1.83
gql[requests]>=3.5

# ---- report ----
matplotlib>=3.8
seaborn>=0.13
streamlit>=1.36
py3Dmol>=2.0
