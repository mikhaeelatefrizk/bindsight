name: bindsight-report
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip>=24.0
  # Upper bounds match pyproject.toml; envs/constraints.txt pins the exact
  # versions this release was tested against.
  - pandas>=2.2,<3
  - pyarrow>=16,<25
  - matplotlib>=3.8,<4
  - seaborn>=0.13
  - jupyter>=1.0
  - quarto>=1.5
  - pip:
      - py3Dmol>=2.0
      - streamlit>=1.36
      - -e ..
